package com.harkerpoul.weatherforecast.common

object AppConfigs {
    /**
     * Should define in the .properties file  and use it as baseUrl = BuildConfig.BASE_URL
     */
    const val BASE_URL = "https://api.openweathermap.org/"
}